//! Scaffolding placeholder: VarDCT coefficient decode and dequantization.
//!
//! In the current pipeline this happens inside
//! `jxl_render::RenderContext::render_keyframe` (private `jxl-render` `vardct`
//! module). It will move here once the renderer is forked to consume the
//! per-tile `FramePlan` directly.
