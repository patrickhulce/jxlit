//! Scaffolding placeholder: loop filters (Gabor, EPF), features
//! (patches/splines/noise), and upsampling.
//!
//! In the current pipeline this happens inside
//! `jxl_render::RenderContext::render_keyframe` (private `jxl-render` `filter`,
//! `features`, and upsampling code). It will move here once the renderer is
//! forked.
